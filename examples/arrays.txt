let matrix: num[][] = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];

let element: num = matrix[1][1];
print("Элемент [1][1]:");
print(element);

matrix[2][0] = 99;
let modified_element: num = matrix[2][0];
print("Измененный элемент [2][0]:");
print(modified_element);

print("Итоговая матрица:");
print(matrix);

let sub_array_length: num = arrayLength(matrix[0]);
print("Длина первого подмассива:");
print(sub_array_length);
